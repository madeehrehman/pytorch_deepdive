# Phase 0 — Setup

| Notebook | Topic |
|----------|-------|
| `phase0_setup.ipynb` | GPU check, devices, dtypes, exercise |

Legacy scripts (`verify_gpu.py`, `device_basics.py`) still work but **notebooks are the primary format**.
